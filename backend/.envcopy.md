# backend/.env example
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_key

PORT=3001
FRONTEND_URL=http://localhost:5173

# AWS (S3 + Textract for PDF import)
AWS_REGION=il-central-1
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# Textract is not available in il-central-1. If AWS_REGION=il-central-1, the code
# automatically uses eu-west-1 for Textract. Override with TEXTRACT_REGION if needed.
# TEXTRACT_REGION=eu-west-1

# IAM permissions: s3:PutObject, textract:AnalyzeDocument